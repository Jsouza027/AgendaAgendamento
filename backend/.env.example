# Porta do Servidor (no Render o próprio Render injeta automaticamente a variável PORT)
PORT=8080

# Caminho do Banco SQLite:
# - Localmente: ./data/salao.db
# - No Render com Persistent Disk (Mount Path: /data): /data/salao.db
DB_PATH=/data/salao.db
